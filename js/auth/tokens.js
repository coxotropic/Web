// Funciones para crear y verificar tokens

const createAccessToken = (userId) => {
  // Código para generar token de acceso
};

const createRefreshToken = (userId) => {
  // Código para generar token de refresco
};

const verifyToken = (token) => {
  // Código para verificar tokens
};

module.exports = {
  createAccessToken,
  createRefreshToken,
  verifyToken,
  // Otras funciones de tokens
};
