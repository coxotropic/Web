// Aquí va el código relacionado con la configuración de estrategias
// (por ejemplo, configuración de OAuth, local, JWT, etc.)

module.exports = {
  configureLocalStrategy,
  configureJwtStrategy,
  // Otras funciones
};
