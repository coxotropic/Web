// Funciones de ayuda generales

const hashPassword = (password) => {
  // Código para encriptar contraseña
};

const comparePasswords = (password, hash) => {
  // Código para comparar contraseñas
};

module.exports = {
  hashPassword,
  comparePasswords,
  // Otras funciones auxiliares
};
